# Cursor rules (canonical)

Agent lane policies, hotspots, and claim workflow: **[../docs/agents.md](../docs/agents.md)**.

This `.cursor/` directory is the **tool entry point** Cursor loads automatically. Human-readable mirrors of long-form docs live under `docs/` (minimal root layout).
