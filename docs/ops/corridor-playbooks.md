# Corridor playbooks (stub)

Operational and community playbooks for **dense EU diaspora corridors** (seed → trust density → repeat handoffs). Align product work with [roadmap epics](../roadmap-epics.md) and the marketing roadmap route group under `apps/web/src/app/(marketing)/roadmap/page.tsx`.

## Placeholder sections (expand with ops)

1. **Corridor selection** — criteria (traffic, language overlap, existing community signal).
2. **Ambassador norms** — tie to Terms & Safety; escalation to moderation.
3. **Seeding** — first trips, first listings, catalog coverage.
4. **Trust** — verified bringer rollout, handoff prompts (`apps/web/src/app/(marketing)/safety/handoff-protocol/page.tsx`).
5. **Metrics** — live counts on `/traction`; do not mix with demo mode.

Add runbooks here as you run real corridors; keep PII out of this repo.
