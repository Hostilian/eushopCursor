{
  "name": "@eushop/party",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "partykit dev",
    "deploy": "partykit deploy",
    "build": "node -e \"require('fs').mkdirSync('dist',{recursive:true});require('fs').writeFileSync('dist/build.txt','PartyKit bundles on partykit deploy. Local dev: pnpm dev.');\"",
    "lint": "echo \"no-op\"",
    "typecheck": "node ../../node_modules/typescript/bin/tsc --noEmit"
  },
  "dependencies": {
    "partykit": "^0.0.114",
    "partyserver": "^0.0.71",
    "partysocket": "^1.0.3"
  },
  "devDependencies": {
    "@eushop/config": "workspace:*",
    "typescript": "^7.0.2"
  }
}
